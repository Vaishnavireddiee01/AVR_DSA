class Solution:
    def isArraySpecial(self, nums: List[int]) -> bool:
        c=0
        b=False
        if len(nums)==1:
            
            return True

        for i in range(0,len(nums)-1):